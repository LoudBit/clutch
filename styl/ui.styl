#clutch {
  height: 100%;
  width: 100%;
  position: absolute
}

#ui {
  background: rgba(0,0,0,0.8);
  padding: 16px;
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  width: 256px;

  ul.tabs {
    list-style: none;
    margin: 0;
    padding: 0;

    li {
      color: rgba(255,255,255,0.5);
      cursor: pointer;
    }

    li:hover {
      color: rgba(255,255,255,0.75);
    }

    li.selected { color: white; }
  }

}

#palette {
  display: block;
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 256px;
}

#anchor {
  box-sizing: border-box;
  height: copy-line-height;
  width: copy-line-height;
  border-radius: copy-line-height*4;
  position: absolute;
}

#spectrum {
  width: 100%;
  height: 100%;
}

#spectrum .spec {
  box-sizing: border-box;
  height: copy-line-height;
  width: copy-line-height;
  border-radius: copy-line-height*4;
  position: absolute;

  webkit-box-flex: 1;
  -webkit-flex-grow: 1;
  -ms-flex-positive: 1;
  flex-grow: 1;
  -webkit-flex-basis: 0;
  -ms-flex-preferred-size: 0;
  flex-basis: 0;
}

#spectrum .spec.anchor {
  // border: 4px groove transparent;
  // box-shadow: 0 0 2px 2px rgba(0,0,0, 1), 0 0 4px 4px rgba(255,255,255, 1);
}
